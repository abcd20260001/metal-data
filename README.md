# Metal City Price Data

Public JSONL snapshots written by `marko898911/metal-city-price-tracker`.

Files are written under:

```text
data/YYYY/MM/DD.jsonl
```

Each line is a JSON price record for one target at one collection time.
